    # Server Port
    PORT=5000

    # CORS Allowed Origin (Update this to your frontend URL in production)
    CORS_ORIGIN=http://localhost:5173

    # Database Configuration (Option 1: Individual Variables)
    DB_HOST=ballast.proxy.rlwy.net
    DB_USER=root
    DB_PASSWORD=WTkZqdgEBFAHKdEZVOPIBSvNMOqzbZao
    DB_NAME=ers
    DB_PORT=21222

    # Database Configuration (Option 2: Connection String - Preferred for Render/Heroku)
    # If DATABASE_URL is provided, it takes precedence over the individual variables above.
    # DATABASE_URL=mysql://user:password@host:port/database
